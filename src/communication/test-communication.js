/** Curl test functions */

const url    = "localhost....";
const cmd    = "curl -X ";
const get    = "GET ";
const push   = "PUSH ";
const delite = "PUT ";


test('Create and destroy request', () => {
    var createUser = ""; // use client interface instead

    // curl 'http://localhost:3000/serviceRequests/myRequest/1'
   expect(
       //

   ).toBe("");
});
